#include "main.h"
/**
 * _abs - absolute value
 * @n - the number to be used in the function
 * @n: the number to be used in the function
 * Description: this function returns the absoulte
 * value of a number(n) passed to it
 * Return: int - absolute value
 */
	int _abs(int n)
	{
		if (n >= 0)
		{
			return (n);
		}
		return (-n);
	}
